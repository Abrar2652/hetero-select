# Core runtime dependencies for HeteRo-Select.
# Tested with Python 3.10 / 3.11 on Linux + CUDA 12.

torch>=2.1
torchvision>=0.16
numpy>=1.24

# Optional — only needed for scripts/make_figures.py and YAML configs.
matplotlib>=3.7
pyyaml>=6.0
